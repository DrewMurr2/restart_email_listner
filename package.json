{
  "name": "scheduledmessagewebjob",
  "version": "1.0.0",
  "description": "Sends the scheduled messages every minute",
  "main": "run.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request": "^2.83.0"
  }
}
